#pragma once

/*
 * Keep both Bluetooth host selections undefined. The Mesh source host test
 * exercises the production transfer state machine directly and does not boot
 * an ESP Bluetooth controller.
 */
